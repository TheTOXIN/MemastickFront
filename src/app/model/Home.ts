export class Home {
  constructor (
    public nick: string,
    public day: number,
    public memes: number,
    public countItems: number,
    public countBells: number,
    public selectTimer: string
  ) {

  }
}
