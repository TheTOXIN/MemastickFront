import {environment} from '../environments/environment';

export const VERSION = environment.version;
export const BACK_URL = environment.backUrl;
export const FRONT_URL = environment.frontUrl;
export const ANDROID_URL = environment.androidUrl;
export const VK_CHAT = environment.vkChat;

export const ACCESS_TOKEN_TIME = environment.accessTokenTime;
export const REFRESH_TOKEN_TIME = environment.refreshTokenTime;

export const ACCESS_TOKEN_KEY = 'access_token_meme';
export const REFRESH_TOKEN_KEY = 'refresh_token_meme';

export const DONAT = 'https://donate.stream/memastick';
export const VK_GROUP = 'https://vk.com/memastick';
export const TELEGRAMM = 'https://teleg.run/memastick';
